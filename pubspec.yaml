name: opentada_flutter
description: "OpenTADA - Text Adventure Development Architecture"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_generative_ai: ^0.3.0  # Updated from ^0.2.0
  file_picker: ^6.1.1
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  markdown_widget: ^2.3.0
  flutter_markdown: ^0.6.18
  openai_dart: ^0.4.5 # Replace with the actual latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
